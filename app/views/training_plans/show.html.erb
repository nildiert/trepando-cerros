<div class="container mx-auto p-4 space-y-4">
  <h2 class="text-xl font-semibold"><%= @training_plan.name %></h2>
  <p><%= simple_format(@training_plan.description) %></p>
  <%= link_to 'Volver', training_plans_path, class: 'btn btn-secondary' %>
</div>
