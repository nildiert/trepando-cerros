<% unless turbo_frame_request? %>
  <div class="container mx-auto p-4 space-y-6">
<% end %>
<turbo-frame id="role_permissions">
  <%= render 'permissions_form', role: @role, permissions: @permissions %>
</turbo-frame>
<% unless turbo_frame_request? %>
  </div>
<% end %>
