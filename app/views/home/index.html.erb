<div class="min-h-screen flex flex-col items-center justify-center p-8">
  <div class="text-center space-y-4 mb-8">
    <h1 class="text-4xl font-bold">Trepando Cerros</h1>
    <p class="text-lg text-gray-700">Analiza tus rutas y obtén un tiempo estimado basado en tu rendimiento en Strava.</p>
    <img src="/runner.svg" alt="Corredor" class="w-24 h-24 mx-auto" />
  </div>
  <div class="w-full max-w-md text-center">
    <% if ENV['STRAVA_CLIENT_ID'].present? && ENV['STRAVA_CLIENT_SECRET'].present? %>
      <%= link_to 'Conectar con Strava', strava_connect_path, class: 'btn btn-primary' %>
    <% else %>
      <p class="text-red-600">Configura STRAVA_CLIENT_ID y STRAVA_CLIENT_SECRET en tu <code>.env</code> para conectarte con Strava.</p>
    <% end %>
  </div>
</div>
